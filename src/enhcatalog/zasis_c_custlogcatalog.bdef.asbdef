projection;
strict ( 2 );
use draft;

define behavior for ZASIS_C_CUSTLOGCATALOG alias CustLogCatalog
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}